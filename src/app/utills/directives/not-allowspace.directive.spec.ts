import { NotAllowspaceDirective } from './not-allowspace.directive';

describe('NotAllowspaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NotAllowspaceDirective();
    expect(directive).toBeTruthy();
  });
});
